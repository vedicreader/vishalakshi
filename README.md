# vishalakshi


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## Install

``` sh
pip install 'vishalakshi[all]'   # + rishi to answer, + kosha & rgapi for code, + mcp for the server
pip install vishalakshi          # vault + acquisition only; no LLM, no MCP
```

## The loop

Everything in this section runs. The corpus is this repository’s own documentation and source, so
the page is reproducible from a clone, and the vault is a throwaway file rather than your real one.
[`Vault()`](https://vedicreader.github.io/vishalakshi/core.html#vault) with no argument uses `~/.vishalakshi/vault.db`.

``` python
from tempfile import mkdtemp
from vishalakshi import Vault

v = Vault(Path(mkdtemp())/'vault.db')
v.enc.note
```

Ingestion is one call. `add` takes a directory, a file, or text, and routes on which it got.

``` python
v.add('..')                       # README.md and every notebook under nbs/
v.note('federate fuses the legs by rank because they share no vector space: the vault embeds '
       'prose, kosha embeds identifiers, ripgrep embeds nothing.', tags=['retrieval', 'design'])
```

Then reach outward. `grab` routes on what the target is: an arXiv id, a YouTube link, a GitHub
repo, a PDF, a local file, a directory. It is the one call a CLI or an agent needs.

``` python
v.grab('1706.03762')                                       # an arXiv id: metadata + abstract
v.pdf('https://arxiv.org/pdf/1706.03762',                  # and the full paper, page by page
      title='Attention Is All You Need')
```

    [2026-08-11 07:21:06] INFO: Fetched (200) <GET https://arxiv.org/pdf/1706.03762> (referer: https://www.google.com/)
    Dictionary used where Stream expected, treating as empty stream
    Dictionary used where Stream expected, treating as empty stream
    Dictionary used where Stream expected, treating as empty stream
    Dictionary used where Stream expected, treating as empty stream

    {'doc_id': '47e4bf9c7b60a727',
     'title': 'Attention Is All You Need',
     'kind': 'pdf',
     'nodes': 5,
     'chunks': 102}

Now ask it something. The model is rishi’s own default, `gemma4_e2b`: LiteRT, CPU, no API key.
Naming a model is naming it to rishi, so an id, a path or a `runtime/...` prefix all work, and
`chat_kw=` reaches the rest of rishi’s constructor. There is no model registry here to fall out of
date.

The defaults hand the model four sections at 1500 characters each. That is the point of retrieving
*sections* rather than documents: the answer needs the paragraphs that bear on the question, not the
corpus. A pointed context is why a 2B model on a laptop can answer at all.

``` python
from rishi.litert import gemma4_e2b

r = v.ask('why are rankings fused instead of distances?', model=gemma4_e2b)
print(r.model, '·', r.runtime)
print(r.answer)
```

    litert-community/gemma-4-E2B-it-litert-lm · litert
    Reciprocal Rank Fusion (RRF) is used because the components (legs) in the system do not share a vector space, meaning they cannot be merged by distance [3]. The vault embeds prose, kosha embeds identifiers, and ripgrep embeds nothing, so the legs cannot be merged by distance [3]. RRF only needs each leg's *ordering*, which is what survives a change of encoder, and it is the same mechanism that Litesearch uses to combine Full-Text Search (FTS) with vectors [3]. Each leg is tried independently to report what it contributed or why it did not [3].

Every `[n]` in the answer resolves to a `node_id` you can read. That round trip is the point: a claim you can check against the text it came from.

``` python
for c in r.cited: print(c['n'], c['breadcrumb'])
print()
print(v.read(r.cited[0]['node_id'])['text'][:400])   # the exact text behind the claim
```

    3 03 code › Fusing legs that share no vector space

    The vault embeds prose, kosha embeds identifiers, ripgrep embeds nothing, so the legs cannot be
    merged by distance. Reciprocal Rank Fusion needs only each leg's *ordering* — which is exactly
    what survives a change of encoder — and it is the same mechanism litesearch already uses to
    combine FTS with vectors. Each leg is tried independently: `legs` reports what each contributed,
    or why it did not.

## What is decided, and what is yours

The retrieval defaults are litesearch’s, and they are measured rather than chosen: `evals/` runs 120
known-item queries per genre over three corpora and scores section-level MRR. You inherit the whole
ladder by opening a vault.

| decided for you | why |
|----|----|
| 512-character chunks | +0.06 to +0.12 weighted MRR over page-sized |
| `pre()` on the keyword leg | +0.016 to +0.093 |
| HNSW vector index | -0.005 quality for a large speedup |
| a document tree, always | ranking is a wash, and `toc`/`read`/`sections`/`context` come free |
| one static encoder, 256d, float16 | the spread across four encoders is 0.018 to 0.046, and the static one wins a genre at ~1,700x cheaper indexing |
| Devanagari and IAST fold to the same token | 1.000 verse recall for every encoder tested, from the tokenizer rather than the embedding |

Four things are yours, and none of them is required to get an answer.

| yours | what it costs |
|----|----|
| `rerank=True` on `search`, `sections`, `context` | +0.026 to +0.077 weighted MRR, positive in all twelve paired cells, at roughly 10x the query latency |
| `shelf(name)` | a partition, so two corpora stop diluting each other’s ranking |
| `llm=` on `categorize` and `extract` | the cue table answers most documents for free; a model is for the ties |
| `db.graph_search` | the graph leg by name, for bridge queries |

The graph leg is off for ranking and stays off. On ordinary known-item questions it costs 0.070 to
0.160 weighted MRR, negative in every genre and flavour measured. It wins only when the answer
shares no word with the question. `connect()` is still worth running, for the reason below.

Sanskrit is the one place something switches itself on. Ingest a Sanskrit file and the vault
re-registers litesearch’s readers with vidyut lemmas and Monier-Williams glosses, because putting
the English behind the Sanskrit into the index beat changing the encoder when it was measured on
this vault’s own shelf: a static encoder with glosses beat a 300M ONNX transformer without them. It
is an ~83 MB download, paid once, only by someone actually reading Sanskrit.

## Everything in one corpus

Every document carries a `kind`: `web`, `pdf`, `arxiv`, `youtube`, `file`, `code`, `data`, `note`,
or whatever litesearch’s parser called the file. Filter when you want to, don’t when you don’t.

``` python
v.stats()
```

``` python
len(v.search('rank fusion')), len(v.search('rank fusion', kind='note')), len(v.search('rank fusion', kind='md,notebook'))
```

The filter is a SQL `WHERE` pushed into the search, not a pass over the results afterwards, so a
narrow filter over a large vault still returns a full page of hits.

Notes are ordinary documents, deliberately. The graph, the clusters and `context()` all see them for
free, so what you concluded about a corpus comes back next to the evidence you concluded it from.

``` python
L(v.sources()).map(lambda d: (d['kind'], d['title'], d['source']))[:6]
```

## When the graph and the clusters pay off

`connect()` builds an entity graph over everything in the vault, and `map()` reads the topics it
persisted. Neither is retrieval. The graph leg loses on ordinary queries, which is why it is not
wired into `search`.

What they answer is the question you ask *before* you know what to search for. `map()` is the shape
of what you have collected, which is how you notice that four months of reading turned into three
subjects rather than the one you thought. The graph is for reaching across shelves and across
documents that never share a word: a bridge query where the paper and the invoice both mention a
company neither of them names in the same way. Run `connect()` after a batch of ingests rather than
inside each one, since it rebuilds over the whole vault.

``` python
v.connect()
```

``` python
# fast after connect() — reads persisted topic nodes from the graph rather than re-clustering
v.connect()
L(v.map().clusters).map(lambda c: (c['size'], c['label'], L(c['members']).map(lambda cc: cc['content'][:40])))
```

    [(8, 'cues, ents, needs, org', [':id|no)\\b|\\bauth(?:orisation|orization) ', "\\b|\\bdimensions\\b', r'\\badd to (?:cart|b", "    'report': dict(needs=(), ents=('ORG'", "    'invoice': dict(needs=('money',), en", ":no|number|#)|\\bestimate\\b', r'\\bvalid (", "    'email': dict(needs=('email',), ents", "\\b|\\bapi reference\\b', r'\\bparameters?\\b", "        r'\\bwork experience\\b|\\bemployme"]), (8, 'loading, traceback, beats, add_tree', ['py` files into the prose store instead —', 'py` files into the prose store instead —', "  'n_docs': 15}\n```\n\nFiling `.py` files ", 'anything, so a question about late chunk', 'anything, so a question about late chunk', 'The graph is rebuilt once at the end rat', 'A repo is prose *and* code, and the two ', 'A repo is prose *and* code, and the two ']), (8, 'sec, filename, cited, node_id', ['results"]), (3, \'rat, fetched_at, resear', "[a-z]*):']), (2, 'stem, path_or_url, ver", "              note:str='',         # a l", '    text, think = split_think(text)\n    ', "res = L([AttrDict(node_id='d#1', title='", "create`, and the clone is already o', ' ", 'read(node_id, max_chars=max_chars), self', "60 |\\n\\nTAX INV']), (7, 'owed_to, due, w"]), (8, 'ranked, lists, pagerank, fusion', ["write_text('# fuse\\n\\nRanks are fused be", '# and `context` splices them in *after* ', '`v.code(dir)` files source files as ordi', '`v.code(dir)` files source files as ordi', 'info      # pagerank, degree, callers, c', 'info      # pagerank, degree, callers, c', "test_eq(_x.document(_p).origin, 'disk')\n", " The corpus is thi', 'Every `Vault` meth"]), (8, 'useful, fields, paperwork, common', ["     'vendor_tax_id',\n     'bill_to',\n  ", '`extract` reads a whole document and ret', '`extract` reads a whole document and ret', '`extract` reads a whole document and ret', "md')      # no schema: the doctype picks", "md')      # no schema: the doctype picks", 'fields it recovers are the fields you as', 'fields it recovers are the fields you as']), (8, 'doctype, save, schema, chat_kw', ['    "Pull structured fields out of one d', '                limit:int=None,     # st', '        sch = SCHEMAS.get(dt, Summary)\n ', '                    fields=flds, chars=l', ' The currency belongs in its own field.\n', '            max_chars:int=2_000_000,  # ', "append(dict(doc_id=r['id'], title=r['tit", 'read straight off it.\n\n#| export\n@patch\n']), (8, 'docs, doc_id, self, attrdict', ["    'Type every document in the vault th", 'def of_type(self:Vault, doctype:str, kin', "doc_id}#0' if d.doc_id else '', title=d.", "append(AttrDict(node_id=s['node_id'], ti", 'retrieval filters on, chosen by whatever', "    'What kind of document this is: invo", '    what you have, then call `connect()`', 'doc_id, doctype=dt, doctype_by=by, docty']), (8, 'watches, watch, action, target', ["v.watch('https://example.com/changelog',", "v.watch('https://example.com/changelog',", "``` python\nv.watch('https://example.com/", "    return dict(watch_id=w['id'], action", '          **params            # forwarde', '    return dict(row, params=params)\n\n@pa', ' `poll` rebuilds the entity graph once a', '             last_run=float, last_status']), (8, 'vishalakshi_vault, command, uses, html', ['`vishalakshi-mcp` exposes the vault to a', '`vishalakshi-mcp` exposes the vault to a', '`vishalakshi-mcp` exposes the vault to a', 'Everything below runs. The corpus is thi', 'Everything below runs. The corpus is thi', 'io/vishalakshi/core.html#vault) method i', "enc = encoder if _is_enc(en', '`store` o", 'Every `Vault` method is a command, and e']), (8, 'target, url, ghfile, arxiv', ['index_code(d))\n    return dict(self.add(', "    if kind == 'arxiv':    return v.arxi", "    'File one file from GitHub: prose in", '   return dict(source=url, kind=\'co", \'s', '    "Read a YouTube video\'s transcript a', '    depends on what is inside the file r', "search(r'youtube\\.com|youtu\\.be', target", '    items = (paginate_api(ep, payload=ca']), (8, 'retailer, browse, www, dairy', ['Listing, product and dashboard pages ren', 'Listing, product and dashboard pages ren', 'Listing, product and dashboard pages ren', "v.harvest('https://www.example-retailer.", "v.harvest('https://www.example-retailer.", 'Listing, product and dashboard pages ren', '# which is where `apis` counted them — w', 'find(\'free range eggs\')     # each pro",']), (8, 'pyfencecallback, fence, claim, merge', ['from rishi import Chat, PyFenceCallback,', 'from rishi import Chat, PyFenceCallback,', '    def merge(*ds):\n        "Merge all d', 'The question "how does `merge` handle a ', 'The question "how does `merge` handle a ', 'runtime)\nprint(r.answer)\n\nEvery `[n]` in', ' In the provided example, since `dict3` ', ' Each leg is evaluated independently to ']), (8, 'embeds, identifiers, ripgrep, legs', [' The vault embeds prose; kosha embeds\nid', ' The vault embeds prose; kosha embeds\nid', "     'RRF over prose, shelf:papers, shel", 'The vault embeds prose, kosha embeds ide', 'free, so what you concluded about a corp', "')                  # README.md and ever", '    The vault embeds prose, kosha embeds', "note('federate fuses the legs by rank be"]), (8, 'mk_encoder, enc_spec, test_eq, float16', ['test_eq(enc_spec(None)[0], DFLT_ENC)\ntes', "# litesearch's model dicts are AttrDicts", "simplefil', '__name__}: {str(e)[:100]});", 'class _Fwd:                       # a mo', "test_eq(v.set_meta(d['id'], reviewed=Tru", 'catch_warnings():\n    warnings.simplefil', '    except Exception as e:\n        warni', "stats()\n\ntest_eq(v.stats()['docs'], 2)\nt"]), (7, 'chosen, contract, everywhere, unanswerable', ['`ask` retrieves sections from everywhere', '`ask` retrieves sections from everywhere', '`ask` retrieves sections from everywhere', '`ask_doc` is `ask` with `ref=` filled in', "\\n\\n#| export\\n@patch\\n', '            m", "    'question about the user\\'s own syst", '`schema` turns the answer from prose int']), (7, 'force, patch, add_file, meta', [' `force=True` re-ingests a source alread', "get('pdf_path'),fetched_at=time.time())\n", '            for p in files:\n            ', '    if (doc := get_pdf(path_or_url, **kw', "    'Ingest one local file into the vaul", "    if enc == 'hash': enc, kw = None, di", "add(md, clip(v['title']), source=url, ki"]), (7, 'inbox, owed_to, due, acme', ["test_eq(a.cited.attrgot('node_id')[0], f", '# empty directory turns the hit above in', '# v.ask_doc(REFS, Q, doc_chars=60000, mo', '# v.ask_doc(REFS, Q, doc_chars=60000, mo', "answer), ('Answer', ['amount', 'currency", '    return schema(**{k: v for k, v in d.', "test_eq(_x.categorize('/inbox/acme-0117."]), (7, 'string, __doc__, get_origin, fields', ["               'list[str]':list[str], 'l", "    return make_dataclass(re.sub(r'\\W', ", '    "A schema written out for a model to', '\ndef _norm(obj, schema) -> dict:\n    "A ', "categorize(r['doc_id'], llm='never')\npri", "categorize(r['doc_id'], llm='never')\npri", '# schema rather than getting a near-dupl']), (7, 'reference, a4_page, font, write_bytes', ['a4_page(), 760.\nfor txt, sz in LINES:\n  ', 'a4_page(), 760.\nfor txt, sz in LINES:\n  ', 'a4_page(), 760.\nfor txt, sz in LINES:\n  ', "\\nfor txt, sz in LINES:\\n  ', 'a4_page()", "test_eq(v.doc(r.doc_id)['meta']['doctype", "match(t):\n\t        parts.append('#'*min(", 'money and reference numbers, entity labe']), (7, 'papers, shelves, test_eq, late', ["shelves().attrgot('store')\ntest_eq(e.att", "e = v.elsewhere('chunk embeddings')\ntest", "read(node_id, max_ch']), (3, 'clusters, ", "test_eq(secs('1h30m'), 5400); test_eq(se", "test_eq((sh.store, sh.db is v.db), ('pap", "r = v.connect()\nassert r['entities'] > 0", "pdf').text.startswith('# Late chunking')"]), (7, 'arrived, answers, yet, llm', ['shape of what you have collected — the s', 'shape of what you have collected — the s', "``` python\nv.categorize_all(llm='never')", ' `by` records which leg answered, so a\nv', '`kind` says how a document arrived. What', '`kind` says how a document arrived. What', '`kind` says how a document arrived. What']), (7, 'layers, decoder, position, layer', [' To facilitate these residual connection', 'Figure 1: The Transformer - model archit', '", "y\', [\'Listing, product and dashboard', ' In a self-attention layer all of the ke', ' We implement this inside of scaled dot-', '- In "encoder-decoder attention" layers,', ' Similar to the encoder, we employ resid']), (6, 'loads, meta, replace, forget', ['def set_meta(self:Vault, doc_id:str, **k', 'add(list(enumerate"]), (8, \'meta, loads,', 'doctype if c else None,\n                ', "    'One whole document, reassembled in ", 'def sources(self:Vault, kind:str=None) -', "    'make up on the spot — and `ask_doc`"]), (6, 'recs, records_md, urls, captured', ['           harvested_at=time.time())), e', '`force=True`, and is otherwise a no-op —', '                    kind=kind, force=for', '             for i, h in enumerate(self.', "warn(f'could not index {len(srcs)} sourc", "    return '\\n\\n'.join(f'## {ttl(r)}\\n\\n"]), (6, '_ask, _chat, classify, chat', ["            f'no recorded reply for {key", 'sp, **self.kw)\n        return self._chat', 'recorded reply for {key", \'sp, **self.kw', '_ask(f\'{self.model}|classify|{sp}|{",".j', '                      lambda: (lambda o:', 'Chat` on a miss\n    ):\n        from disk']), (6, 'blob, com, https, calls', ['calls = []\nsaved = {n: getattr(Vault, n)', "com/post');                             ", "test_eq(what_is('https://github.com/Answ", 'import fossick\n_saved = fossick.read_gh_', 'stats()[\'docs\'], 2)\\nt", \'# raised Impor', '    # the source branch never fetches: t']), (6, 'eggs, range, milk, dairy', ["v.find('free range eggs')     # each pro", "v.find('free range eggs')     # each pro", "find('free range eggs')     # each produ", "v = Vault(':memory:')\nv.add_records([dic", "find('free range eggs', limit=2)).attrgo", "find('free range eggs', limit=2)).attrgo"]), (6, 'failed, less, retry, _boom', ['seen   # the second prompt really is sma', '        if len(_Boom.seen) == 1: raise R', '# less — on a *new* chat. `hist = []` cl', '# a recorded failure is replayed as a fa', ' A prompt too long for the window comes ', '__name__, structured(ch, prompt, sch, sp']), (6, 'think, mlx, citations, constructor', ['# own error when the id says nothing abo', 'vector *width* disagrees, but when the w', "', model=gemma4_e2b)             # an id", 'The vault wants a real embedder and will', '`mlx/…` prefix — raises rather than gues', "Thinking is rishi's too — every backend "]), (5, 'rejects, hard, nested, cpu', ['A predefined schema works the same way —', 'A predefined schema works the same way —', 'A predefined schema works the same way —', "       'unit_price': '$1.20',\n       'am", 'Those are typed Python values, not prose']), (5, 'went, rglob, children, land', ['The PDF went in page by page, so the tre', 'The PDF went in page by page, so the tre', 'The PDF went in page by page, so the tre', "document('Attention', max_chars=20).trun", "      'attention', source='/inbox/attent"]), (5, '_json_reply, json, funccall, get_schema', ['structured(prompt, schema, sp=sp), schem', 'def _json_reply(ch, prompt:str, schema, ', "qv(q), limit=limit, s', 'def route(self:", 'test_eq(as_schema(Invoice), Invoice)    ', 'from fastcore.funccall import get_schema']), (5, 'bear, paragraphs, laptop, retrieving', ['the whole point of retrieving *sections*', ' There is no model registry here to fall', 'the whole point of retrieving *sections*', "shelf's, so they arrive as extra section", "li']), (2, 'data, nodes, modernbert, vdw"]), (5, 'dims, store, mk_encoder, database', ["store = str(ifnone(path, Path.home()/'.v", "        if r and (r['encoder'], r['dims'", '@patch\ndef find(self:Vault,\n         q:s', 'method, added_at=now), replace=True)\n   ', '                 path:str=None,       # ']), (5, '_fs, add_files, offline, fusion', ['# written one document at a time have to', "stats()['docs'], 6)\nassert vb.find('rank", 'from tempfile import mkdtemp\ntd = Path(m', '# different place to keep the same numbe', "test_eq(r['title'], 'fusion notes')     "]), (5, 'subtotal, unit, qty, vat', ['hours of compute to answer a question a ', '| Description | Qty | Unit price | Amoun', '| Description | Qty | Unit price | Amoun', '| TAX | INVOICE | |\n|---|---|---|\n| Acme', '60 |\n| Total | Due: | £849.60 |\n\nTAX INV']), (5, 'step, post, help, adds', ["patch', 'litesearch.rrf_all', 'vishalaks", "code_search', 'vishalakshi.extract.guess", '``` sh\nvishalakshi grab https://example.', '```sh\nvishalakshi grab https://example.c', '```sh\nvishalakshi grab https://example.c']), (5, 'use_chat, settings, vishalakshi_model, new_chat', ["getenv('VISHALAKSHI_MODEL')\n\nCHAT = Chat", '`ask` is one call over four backends, be', '#| hide\n# a stand-in that records what t', '    "Is `new_chat` still building rishi\'', "test_eq(model_cached('no-such-org/no-suc"]), (5, 'api, outputs, laptop, ran', ['Everything below actually ran. The outpu', 'Everything below actually ran. The outpu', 'Everything below actually ran. The outpu', ' Everything else on this page runs with ', ' Everything else on this page runs with ']), (5, 'abstract, used, link, gives', [' Each of these is a real call against a ', '`connect()` is what the associative leg ', '    Dictionary used where Stream expecte', '`grab` routes on what the target *is* — ', "03762')                                 "]), (5, 'preprint, http, abs, org', [' In Proceedings of the 21st Internationa', '[4] Jianpeng Cheng, Li Dong, and Mirella', '03130)\n\n[23] Minh-Thang Luong, Quoc V. L', 'org/abs/1508.07909)[, 2015.](http://arxi', ' In Empirical Methods in Natural Languag']), (4, 'add_tree, splits, kosha, files', ['A repo is prose *and* code, and the two ', 'code embedder.\n\n#| export\n@patch\ndef add', " `find` locates things, '\n    '`read` pu", 'holds both, and remembering to call each']), (4, 'filling, forget, accumulates, agent', ["description are the method's signature a", ' `context` and `find` for reading, `ask`', 'description are the method’s signature a', "fossick's `json_records` walks for the l"]), (4, 'select, prefix, topic, chunks', ['        return np.asarray(self.enc.query', "    'Row counts across the vault, by kin", '                        chars=len(txt), ', '    res = build_graph(self.db, _paged(se']), (4, 'declared, works, due_date, string', ['A shape you have not declared works the ', 'A shape you have not declared works the ', 'A shape you have not declared works the ', 'c"]), (8, \'string, vendor, due_date, flo']), (4, 'action, target, run_watch, reminder', [' The documents it already filed stay in ', '    try: return float(every)\n    except ', "               if w['action'] == 'remind", 'A watch is what turns the vault from an ']), (4, 'min_score, min_margin, sig, margin', ['            # verdict stands, and says w', '    return AttrDict(doctype=top if best ', '    return dict(sorted(out.items(), key=', '# a YouTube ingest *is* a transcript. On']), (4, 'index_code, fills, packages, files', ['`v.code(dir)` files source files as ordi', ': 100%|██████████| 7/7 [00:00<00:00, 385', "        test_eq(r['kind'], 'code')\n     ", 'no vector space, and `federate` fuses th']), (4, 'next_run, action, target, thinki', ['simplefilter(\'always\')\\n    "]), (3, \'pc', '", "fields[k] for k in (\'vendor\', \'total', 'com/changelog\\\',",\', "    return dict(wa', " What', 'shape of what you have collecte"]), (4, 'normalised, federated, mod, ref', ['join(s[\'snippets\'])[:600], open=f"read({', "get('content') or '')[:600],\n           ", "    if not getattr(v, 'federate', None):", 'by a multilingual model are as incompara']), (4, 'tie, seam, decisive, acm', ['SKU: GN-220. Price: $1.20 per unit. Out ', "add(INVOICE, 'Acme invoice ACM-2024-0117", '| Description | Qty | Unit price | Amoun', '| Description | Qty | Unit price | Amoun']), (4, 'spec, dims, dtype, isinstance', ['        f = lambda xs, **kw: hash_embed(', "        m = spec if how == 'ready' else ", '    if isinstance(spec, str): spec = [p ', 'def mk_encoder(model=None,          # an']), (4, 'clie, a_str, expecte, nwhich', ["a_str(S)', '# docments, which need *sour", 'store\\n\\nWhich leave"]), (2, \'paragraph,', 'attrgo"]), (2, \'_cc, test_fail, contains', "03762')                                 "]), (4, 'correctly, imports, means, overflows', ['characters (3533 tokens) answers on the ', 'characters (3533 tokens) answers on the ', 'characters (3533 tokens) answers on the ', '*means* — an invoice reassembled without']), (4, 'nid, tier, node_id, answers', ['*why* a document is in your corpus — the', '*why* a document is in your corpus — the', '`related(node_id)` answers the other que', 'Reuses the vectors usearch already holds']), (4, 'currency, entry, paid_with, dataclass', ['    subtotal:float = 0\n    tax:float = 0', 'description of the shape it must fill, s', 'one — and the shape you want is just a s', "    paid_with:str = ''\n    items:list[di"]), (4, 'scored, cue, phrases, matched', ['A cue table, scored. `cue_scores` is a p', '    # label: the cue phrases that are ev', 'leg is dropped and its weight redistribu', 'patterns miss, yake keyphrases fill in a']), (4, 'ever, markdown, refs, ingested', ['`ref` is the other half. With none, the ', 'markdown page or a source file gets aske', 'markdown page or a source file gets aske', 'markdown page or a source file gets aske']), (4, 'findings, education, year, experience', ["    name:str = ''\n    email:str = ''\n   ", ' dates: ISO where the document allows.""', '    findings:list[str] = None\n    limita', ' obligations: what each side must do. Da']), (4, 'transformer, posunk, gnmt, base', ['Table 2: The Transformer achieves better', '| Deep-Att | + PosUnk | Ensemble | [39] ', '16 | 25.1 | 58 |\n|---|---|---|---|\n| (B)', 'Table 3: Variations on the Transformer a']), (4, 'sanskritgemma, _sanskr, paired, _sanskrit_eval', ['A Sanskrit-tuned static model (`encoder=', 'A Sanskrit-tuned static model (`encoder=', 'A Sanskrit-tuned static model (`encoder=', '    if isinstance(_m, dict): test_eq(enc']), (4, 'transduction, sequence, entirely, recurrent', [' We used a beam size of 21 and α = 0 3\n\n', 'To the best of our knowledge, however, t', 'The dominant sequence transduction model', 'Attention mechanisms have become an inte']), (4, 'berlin, gmbh, contoso, net', ['Two documents, one of each kind, and not', 'hours of compute to answer a question a ', 'hours of compute to answer a question a ', ' Entered by hand rather than with `with`']), (4, 'scaled, looking, dot, better', ["    ['Attention Is All You Need']\n    fi", "', limit=3, kind='pdf')).attrgot('breadc", "sections('what is scaled dot-product att", "sections('what is scaled dot-product att"]), (4, 'computation, sequential, output, input', ['Recurrent models typically factor comput', 'length n is smaller than the representat', 'The goal of reducing sequential computat', ' Recent work has achieved significant im']), (3, '_caps, replay_xhr, endpoints, records', ['        if not found: return dict(url=ur', "_caps = [dict(url=_cap['url'], data=_pay", '         **kw                 # forwarde']), (3, 'inbox, nested, fallback, total', ["assert _by.startswith('cues ('), _by\n\n\n#", '0)                       # what the mode', "test_eq(sorted(f), ['total', 'vendor'])\n"]), (3, 'doc_context, _dc, fusion, rank', ["breadcrumb, 'doc 3')\ntest_eq(_dc.results", "_dc2 = _v.doc_context(['doc 3', 'doc 5']", 'Retrieval needs no model; only the answe']), (3, 'chunk_id, entity_id, select, mentions', ["docs(where=w,', 'docs(where=_kw(kind) if", '    if not ents: return None\n    eids = ', "id, doctype=dt, doctype_by=by, docty', '"]), (3, 'labels, page_no, hashed, conn', ['method})")\n\ndef _kw(kind) -> str: return', '        ref:str,              # doc_id, ', 'origin, \'disk\')\\n", \'    def classify(se']), (3, 'open, something, citation, changes', ['`context()` retrieves prose. When the qu', 'budget for the named documents *together', 'Those sections are numbered alongside th']), (3, 'filename, breadcrumb, split, pages', ["split('#')[0], store=nm, title=r['title'", '        bge_model, embedding_gemma, mode', '            shelves:int=2,      # sectio']), (3, 'ctx, mk_prompt, doc_chars, related', ['doc_context(ref, question, related=relat', "warn(f'{type(e).__name__} on a {len(prom", "docs(where=_kw(kind), select='id')}\n    "]), (3, 'topic, freq, clusters, _map_from_graph', ['def map(self:Vault, min_count:int=2, for', 'delete_where("kind=\'topic\'")\n        exc', '    clusters = L(AttrDict(centroid=None,']), (3, 'unknown, ones, fields, field', ['The shapes worth having ready, and `dyn_', ' `list` fields default to `None` rather ', ' Unknown keys are\ndropped and missing on']), (3, 'embed_batch, embedded, database, time', ['    are deferred and embedded `embed_bat', '              embed_batch:int=2000, # ch', 'you read goes in under a `kind`, and one']), (3, 'emit, send_message, small, opaque', ['Those are typed Python values, not prose', 'Those are typed Python values, not prose', 'for, not prose about them.\n\n`structured`']), (3, 'combine, contributed, independently, mechanism', ['survives a change of encoder, and it is ', 'survives a change of encoder, and it is ', '`federate` fuses their *rankings* with R']), (3, 'thought, sec, rel, vault_sp', ['thinking = split_reasoning(resp_text(res', '    return AttrDict(node_id=node_id, ans', 'all import AttrDict, L, patch\nfrom rishi']), (3, '_fid, lists, weights, legs', ['    fused = rrf_all(list(lists.values())', '             shelves=True,      # other ', "store}', lambda s=s, nm=nm: _prose(s, q,"]), (3, 'lbls, mid, auto, lower', ["classify(f'{d.title}\\n\\n{txt}', list(lbl", '        cues = [p for p in _CUES[lbl] if', "    mode = {True: 'always', False: 'neve"]), (3, 'dot, product, subspaces, nheads', ['`offline=True` skips the model download ', 'attrgot(\'breadc", "sections(\'what is sca', "#| eval: false\nVault(encoder='minishlab/"]), (3, 'cambridge, lane, mill, documentbuilder', ["         ('Bill To: Vishalakshi Research", "         ('Bill To: Vishalakshi Research", "         ('Bill To: Vishalakshi Research"]), (3, 'confidently, describe, alone, difference', ['py` alone: retrieval will not reliably p', 'py` alone: retrieval will not reliably p', 'py` alone: retrieval will not reliably p']), (3, 'dead, involved, schedule, stops', ['the name of an acquisition method, so an', 'the name of an acquisition method, so an', ' An action is just\nthe name of an acquis']), (3, 'afterwards, sql, large, pushed', ['Every document carries a `kind` — `web`,', 'Every document carries a `kind` — `web`,', "      'txt': 1,\n      'note': 1,\n      '"]), (3, 'provided, assistant, write, value', ['If this answers the request, reply with ', "``` result\n{'a': 5, 'b': 3, 'c': 4, 'd':", 'If two dicts share a key, which value wi']), (3, 'encountered, multiple, processed, final', ['print(resp_text(r))\n```\n\n    The provide', 'The function merges the dictionaries by ', '**The rule for which value wins is that ']), (3, 'dict1, dict2, dict3, merged_dict', ["# Expected Output Analysis:\n# 'a': 5 (fr", '    return {k:v for d in ds if d is not ', '    "Merge all dictionaries in `ds`"\n   ']), (3, 'counts, labels, kws, noun', ['                  for k in (*counts, *(l', "label not in {'ORG', 'PERSON', 'LAW', 'P", "for k in ('money', 'date', 'ref', 'tax',"]), (3, 'depth, dir, limit, int', ['of — is invisible to both. It reads the ', '    k = self.kosha(dir)\n    return AttrD', '    if shelves:\n        found = self.els']), (3, 'paragraph, pdf_oxide, march, april', ['Paperwork is the case where retrieval is', 'Paperwork is the case where retrieval is', 'Paperwork is the case where retrieval is']), (3, 'bhagavad, latency, mrr, million', ['Vālmīki’s Rāmāyaṇa, straight from GRETIL', "Vālmīki's Rāmāyaṇa, straight from GRETIL", "Vālmīki's Rāmāyaṇa, straight from GRETIL"]), (3, 'server, rgapi, pip, install', ["```sh\npip install 'vishalakshi[all]'   #", "```sh\npip install 'vishalakshi[all]'   #", "``` sh\npip install 'vishalakshi[all]'   "]), (3, 'degrade, spacy, text_entities, got', ["The entity leg is litesearch's own: `tex", "The entity leg is litesearch's own: `tex", 'The entity leg is litesearch’s own: `tex']), (3, 'ingested, pass, skipped, force', ["oc_id': 'e0e6905a18b53f42', 'title': 'in", "4f78c6c7e33f2', 'title': '04 cli', 'skip", "  'docs': [{'doc_id': 'afb308e53f8fc916'"]), (3, 'prepare, generated, modules, pip', ['The notebooks in `nbs/` are the source; ', 'The notebooks in `nbs/` are the source; ', 'The notebooks in `nbs/` are the source; ']), (3, 'disagree, searching, cross, main', ["find('Rāma is exiled to the forest', lim", "find('Rāma is exiled to the forest', lim", 'Searching the main vault does not reach ']), (3, 'set_min_log_severity, litert_lm, importerror, readlin', ["---\ndescription: 'one vault for everythi", '# vishalakshi\n\n\n<!-- WARNING: THIS FILE ', '#| hide\nfrom vishalakshi import *\nfrom f']), (3, 'wmt, ensembles, task, german', [' Experiments on two machine translation ', 'On the WMT 2014 English-to-German transl', ' On both WMT 2014 English-to-German and ']), (3, 'single, narrower, replacement, unreadable', ['A move is a re-ingest: the other shelf h', '`categorize_all` records one failure and', 'question of what *this* document talks a']), (2, 'person, cues, ents, bapplication', ["        r'\\bdecided\\b|\\bdecisions?\\b|\\bo", ':speaker \\d|[A-Z][a-z]+\\s?[A-Z]?[a-z]*):']), (2, 'tin, withholding, sac, ein', ["|receipt|bill|account|sku|part|item)\\s*'", ':def|class|function|const|let|var|import']), (2, 'ndef, field, gra, trun', ["\\n\\n#| export\\n@patch\\ndef add', '`symbo", 'sub(r\'\\\\W\', ", \'        return AttrDict(']), (2, 'forwarded, chat_kw, vault_sp, max_chars', ['            **kw                  # forw', '        sp:str=VAULT_SP,       # system ']), (2, 'live, operative, pick, shapes', ['doc_id, title=d.title, source=d.source, ', '        ref=None,              # documen']), (2, 'docu, compile, invo, coun', ['docu\', " L\']), (2, \'person, cues, ents, ', 'compile(\\n    r\'\\\\b([A-Z][A", "    \'Coun']), (2, 'gap, contents, opening, snippet', ['read(node_id, store=store or self.store,', '                                breadcru']), (2, 'elsewhere, blows, named, max_chars', ["    return f'{which} being asked about; ", "    'The documents `ref` names as sectio"]), (2, 'fastlite, branch, shallow, save_dir', ['`gh_file` hands the code branch to `inde', '    "File a source tree into the vault a']), (2, 'honest, required, nbdev_export, signal', ["    test_eq(set(r) >= {'node_id', 'doc_i", "test_eq(schema(tools['extract'])['requir"]), (2, 'mid, chat_kw, repo_id, scan_cache_dir', ['        from huggingface_hub import scan', '            # documents must not turn a ']), (2, 'hex, tags, splitlines, remember', ['def note(self:Vault,\n         text:str, ', '", "\', sections=3, related=0, code=3, di']), (2, 'fetched_at, bot, md_title, wall', ['strip() or (st or 200) >= 400:          ', "    res = research(query, n=n, engine='g"]), (2, 'unwatch, of_type, poll, categorize_all', ["         'poll unwatch index_code code_s", "\n#| export\nCMDS = ('stats find sections "]), (2, 'set, args, __dict__, telling', ["p = anno_parser(cmd('find'), prog='visha", "_pay = {'data': {'items': [{'a': 1}, {'a"]), (2, '_noun_ents, sub, label, statute', ["_LAW_INST = re.compile(\n    r'\\b([A-Z][A", "    'Countable evidence in one document:"]), (2, 'css, selector, sel, duplicated', ['months later `sources()` still says *why', '         target:str,        # a URL, an ']), (2, 'embeds, identifiers, tags, ripgrep', [' Start with what is already on disk — no', 'from vishalakshi import Vault\n\nv = Vault']), (2, 'gh_clone, subprocess, unlink, happened', ['# passes through. We used to infer that ', 'unlink()                                ']), (2, 'jobs, outp, prof, profile', ['name if (pr := profile_for(p)) else None', '    if not files: return L()\n    pend, w']), (2, 'unit_price, qty, line, description', ['fields it recovers are the fields you as', "5,\n       'amount': 294},\n      {'descri"]), (2, 'lak, sanskrit_fast, papers, pays', ['anything, so a question about late chunk', '    shelf:sanskrit_fast Vālmīki Rāmāyaṇa']), (2, 'shelves, shaped, continue, _error', ['              max_read:int=2000,  # char', '        try: legs[nm] = f()\n        exce']), (2, 'nquestion, leaves, nevery, nprint', ["append('RELATED — not retrieved by the q", ", PyFenceCallback,', '    def merge(*ds)"]), (2, 'uses, drive, sys, default_exp', ['Everything below runs. The corpus is thi', '# mcp\n\n> the vault as MCP tools any clie']), (2, 'routes, sanskrit, route, moves', ['`KIND_SHELF` is where acquisition routes', 'the only thing that survives a change of']), (2, 'heads, arxi, tier, figure', ["c']), (3, 'sql, large, narrow, pushed', ", "ic optimizat', 'org/abs/1508.07909)[, 20"]), (2, 'failure, notice, past, assumed', ['The defaults are the product claim: a fe', 'invent what a 2B model says, and inventi']), (2, 'fall, reaches, gives, four', ['the abstract and the PDF gives you the w', 'the abstract and the PDF gives you the w']), (2, 'await, list_tools, renamed, required', ['tools = {t.name: t for t in await mcp.li', "th what is already on disk — no', 'from "]), (2, 'engine, vishalakshi_record_chat, raises, miss', ['properties are what make it worth having', "CHAT_CACHE = 'chatcache'   # a diskcache"]), (2, 'whitespace, scraped, collapse, urllib', ['import json, re, time, uuid, warnings\nfr', '`__delwrap__` is what makes the docments']), (2, 'downloads, lone, copies, overrides', ['fossick shells out to git and keeps the ', '`what_is` names the kind and `grab` disp']), (2, 'sanskrit, sanskrit_terms, vidyut, is_sanskrit', ['    Each shelf is then batched on its ow', 'count: return dict(entities=0, mentions=']), (2, 'audited, selectively, spending, yet', ['case worth spending one on, which is wha', 'case worth spending one on, which is wha']), (2, 'lives, topic, clustering, provenance', ["L(v.sources()).map(lambda d: (d['kind'],", "L(v.sources()).map(lambda d: (d['kind'],"]), (2, 'weighing, reasoning, think, split_reasoning', ["test_eq(split_reasoning('weighing [1]</t", "ments, deliberate', 'Notes are ordinary "]), (2, 'restrict, leads, offered, assembled', ['        related:int=6,         # associa', 'def context(self:Vault,\n            q:st']), (2, 'hits, federate, grep, vector', ['# a shelf joins as its own leg, because ', 'Each leg is optional and each fails on i']), (2, 'concluded, see, clusters, next', ['Notes are ordinary documents, deliberate', 'Notes are ordinary documents, deliberate']), (2, 'forces, vishalakshi_offline, hashing, encoder', ['`$VISHALAKSHI_OFFLINE` forces the hashin', '`$VISHALAKSHI_OFFLINE` forces the hashin']), (2, 'application, applicationbe, opinion, eos', ['applicationbe\n\nbe just should\n\napplicati', 'in\n\nwillnever perfectbut shouldjust is w']), (2, 'root, embed, constructing, repo_root', ['          dir:str=None,              # r', '`kosha_indexed` is deliberately *not* `K']), (2, 'kyunghyun, yoshua, cho, bengio', ['[5] Kyunghyun Cho, Bart van Merrienboer,', '[1] Jimmy Lei Ba, Jamie Ryan Kiros, and ']), (2, 'scaling, additive, dot, using', ['The two most commonly used attention fun', ' While the two are similar in theoretica']), (2, 'litertlm, fastllm, gguf, cpp', ['| `litert` | Google LiteRT, CPU — **the ', '# ask\n\n> answer from the vault, with cit']), (2, 'enc_spec, static, micro, fidelity', ['stays static and the choice stays yours ', 'com/vedicreader/litesearch) ships, by na']), (2, 'by_lexical, canonical, by_ann, knn', ["     'mentions': 7358,\n     'edges': 253", "  'graph': { 'edges': 2451,\n            "]), (2, 'edited, meant, contextual, embeddings', [' `DOCTYPE_SHELF` is the mapping, and\nlik', 'different embedder gets a **shelf** — sa']), (2, 'userwarning, constrained, retrying, warn', ['py:526: UserWarning: ValueError on a con', '    /Users/71293/code/personal/orgs/vish']), (2, 'heads, figure, many, layer', ['Figure 5: Many of the attention heads ex', 'Figure 3: An example of the attention me']), (2, 'env, generalise, serve, identifier', ['               verbose:bool=False,\n     ', '        if share_encoder and self.enc.mo']), (2, 'image, images, png, page4_2', ['![Image 1 from page 4](images/page4_1.pn', '![Image 1 from page 3](images/page3_1.pn']), (2, 'keyerror, raise, _missing, adjudicate', ['# with two sections and a warning would ', '            # `litert_lm_conversation_se'])]

## What each document is, and what is inside it

`kind` says how a document arrived. What it *is* (an invoice, a price list, a contract, a paper, a
transcript, a source file) is a different question, and one worth answering: a vault that knows
which of its documents are invoices can hand you their totals as a table.

`categorize` answers it with a cue table first and a model only where the table cannot decide. That
order is the design rather than an optimisation. Typing ten thousand documents through an LLM is
hours of compute to answer a question a regex answers about most of them.

``` python
INVOICE = '''# INVOICE

Invoice No: ACM-2024-0117
Date: 2024-03-01
Payment terms: Net 30
Bill to: Contoso GmbH, Berlin
From: Acme Supplies Ltd

## Line items

| Description | Qty | Unit price | Amount |
|---|---|---|---|
| Widget, steel | 12 | $8.50 | $102.00 |
| Gasket, nitrile | 40 | $1.20 | $48.00 |

Subtotal: $150.00
VAT (20%): $30.00
Total due: $180.00
'''

v.add(INVOICE, 'Acme invoice ACM-2024-0117', source='/inbox/acme-0117.md')
r = v.categorize('/inbox/acme-0117.md', llm='never')
r.doctype, r.score, r.decisive, r.by
```

    ('invoice', 1.0, True, 'cues (ner+regex)')

`score` and `decisive` are the seam. A clear winner needs no model; a two-way tie is exactly the
case worth spending one on, which is what `llm='auto'`, the default, decides for itself. It will use
a model it can find and never start a download to get one. `by` records which leg answered, so a
vault’s types can be audited and re-run selectively.

``` python
v.categorize_all(llm='never')   # everything not typed yet; a failure is recorded, not raised
v.doctypes()                    # the shape of the corpus
```

    {'code': 8,
     'invoice': 4,
     'other': 3,
     'documentation': 2,
     'paper': 1,
     'transcript': 1}

Notebooks read as code, the README as documentation, the invoice as an invoice, and no model was
loaded to say so. `force=False` makes that cheap to re-run after an ingest: only what arrived since
is looked at. `ner` reads the entity labels off one document, through the same extractor the graph
runs on.

``` python
v.ner('/inbox/acme-0117.md').ents.map(lambda e: (e.label, e.text))[:6]
```

    [('ORG', 'Contoso GmbH'), ('ORG', 'Acme Supplies Ltd'), ('KEYPHRASE', 'Date'), ('KEYPHRASE', 'Payment terms'), ('KEYPHRASE', 'Net'), ('KEYPHRASE', 'Contoso GmbH')]

## Fields, not prose

`extract` reads a whole document and returns a dict. The shapes for the common paperwork are ready,
and a shape you have not declared is one string at the moment you ask.

``` python
from dataclasses import fields
from vishalakshi.extract import SCHEMAS, as_schema

list(SCHEMAS)
```

    ['invoice',
     'purchase_order',
     'quote',
     'receipt',
     'catalogue',
     'contract',
     'resume',
     'paper',
     'meeting_notes',
     'other']

``` python
[f.name for f in fields(as_schema('invoice'))]
```

    ['number',
     'date',
     'due_date',
     'vendor',
     'vendor_tax_id',
     'bill_to',
     'ship_to',
     'currency',
     'subtotal',
     'tax',
     'total',
     'payment_terms',
     'items']

``` python
[f.name for f in fields(as_schema('vendor:str, total:float, due_date:str'))]
```

    ['vendor', 'total', 'due_date']

With no `schema`, the document is categorised first and the shape follows from what it turned out
to be, which is what makes this useful pointed at a folder of mixed paperwork. rishi constrains the
model to the schema (a forced tool call on the hosted and LiteRT backends, a grammar on llama.cpp, a
parsed JSON reply on MLX) so what comes back is the fields you asked for rather than prose about
them. `extract_all(doctype='invoice')` does the same across every invoice in the vault and hands
back one row each, which is a dataframe away from being useful.

``` python
e = v.extract('/inbox/acme-0117.md', chat_kw=dict(backend=Backend.GPU()))      # no schema: the doctype picks one
e.schema, e.fields['total'], e.fields['items']
```

    ('Invoice',
     180.0,
     [{'description': 'Widget, steel',
       'qty': 12.0,
       'unit_price': 8.5,
       'amount': 102.0},
      {'description': 'Gasket, nitrile',
       'qty': 40.0,
       'unit_price': 1.2,
       'amount': 48.0}])

Same call, one argument different, when a bigger model is worth it. The nested `items` list is where a 2B model on CPU struggles and a hosted one does not.

``` python
e = v.extract(r['doc_id'], schema='invoice', model='gpt-4.1-nano')   # needs OPENAI_API_KEY
{k: e.fields[k] for k in ('vendor', 'total', 'payment_terms')} | {'items': e.fields['items']}
```

    {'vendor': 'Acme Supplies Ltd',
     'total': 849.6,
     'payment_terms': 'Net 30',
     'items': [{'description': 'A4 Copier Paper',
       'qty': 12,
       'unit_price': 24.5,
       'amount': 294},
      {'description': 'Toner Cartridge XL',
       'qty': 3,
       'unit_price': 86,
       'amount': 258},
      {'description': 'Desk Lamp', 'qty': 5, 'unit_price': 31.2, 'amount': 156}]}

## Asking about one document, with the vault as context

`ask` retrieves sections from everywhere. Give it `ref=`, or call `ask_doc`, which is the same call
with the name said out loud, and it starts from documents you have already chosen, reads them, and
adds a few retrieved sections behind them. They are sections `[1..n]`, so the citation contract is
the one `ask` already keeps.

Name more than one when the question is a comparison. “What does `extract.py` do that `core.py` does
not” is unanswerable from `extract.py` alone: retrieval will not reliably put the other file in
front of the model, and a model handed one file will confidently describe that file and guess at the
difference. `doc_chars` is the budget for the named documents together, shared between them, because
a context window is a total. Two files at the default 8000 is 4000 characters each.

That default is measured. Through this path on `gemma-4-E2B-it-litert-lm`, 8000 characters (3533
tokens) answers on the first send and 10000 (4265) overflows and needs the retry. Which means two
source files at the local default are truncated to their imports, and a 2B model will say, correctly,
that it cannot tell them apart from what it was given. That is the system working: raise `doc_chars`
and use a model with the window to match, and the same call answers.

Neither document need be in the vault. A path on disk is read straight off it, which is how a
markdown page or a source file gets asked about before it is ever ingested.

``` python
REFS = ['../vishalakshi/extract.py', '../vishalakshi/core.py']   # files on disk, never ingested
Q = 'what does extract.py do that core.py does not?'

a = v.ask_doc(REFS, Q,model=gemma4_e4b,chat_kw=dict(backend=Backend.GPU()))                                   # the default local model: 4000 chars a file
print(a.answer)
# ...and with room for both files, which is what the comparison actually needs:
# v.ask_doc(REFS, Q, doc_chars=60000, model='gpt-5.6-luna')
```

    `extract.py` is a file that defines what a document is, the fields inside it, and an answer over the whole of it [1]. It contains definitions for various document types such as `Invoice`, `Receipt`, `Catalogue`, `Contract`, `Resume`, `Paper`, `MeetingNotes`, and `Summary` [1].

    `core.py` is a file that contains the vault, which is one SQLite file holding everything you have read, and the retrieval over it [2]. It also contains definitions for

And the same question answered as data instead of prose. `schema=` turns any question into a structured response, built at the moment you ask it.

``` python
v.ask_doc('/inbox/acme-0117.md', 'what is owed, to whom, and by when?', model=gemma4_e4b,chat_kw=dict(backend=Backend.GPU()),
          schema='amount:float, currency:str, owed_to:str, due:str').fields
```

    /Users/71293/code/personal/orgs/vishalakshi/vishalakshi/extract.py:527: UserWarning: ValueError on a constrained call for Answer (model neither called the tool nor returned JSON; reply: 'The total amount owed is **$180.00** [1]. It is owed to **Acme Supplies Ltd** [1]. The paymen) — retrying as a JSON reply.
      warnings.warn(f'{type(e).__name__} on a constrained call for {schema.__name__} '

    {'amount': 180.0,
     'currency': 'USD',
     'owed_to': 'Acme Supplies Ltd',
     'due': '2024-03-01'}

## Code, and the two indexes over one tree

A repo is prose and code, and the two want different indexes. `add_tree` splits it: documents to the
vault, source files to kosha, which builds AST chunks, symbol names and a call graph with PageRank.
`grab` routes a directory here, so the one-call path gets it too.

Once a repo is indexed, `context` stops needing to be asked. It appends code sections to what it
retrieves, decided by looking for `.kosha/code.db` on disk rather than by loading anything, so a
question about late chunking pays nothing for a leg it has no use for.

``` python
m=v.index_code('..')                              # this repo; env=True also indexes installed packages
```

    parse files from ..: 100%|██████████| 7/7 [00:00<00:00, 211.58it/s]

<style>
    progress { appearance: none; border: none; border-radius: 4px; width: 300px;
        height: 20px; vertical-align: middle; background: #e0e0e0; }
&#10;    progress::-webkit-progress-bar { background: #e0e0e0; border-radius: 4px; }
    progress::-webkit-progress-value { background: #2196F3; border-radius: 4px; }
    progress::-moz-progress-bar { background: #2196F3; border-radius: 4px; }
&#10;    progress:not([value]) {
        background: repeating-linear-gradient(45deg, #7e7e7e, #7e7e7e 10px, #5c5c5c 10px, #5c5c5c 20px); }
&#10;    progress.progress-bar-interrupted::-webkit-progress-value { background: #F44336; }
    progress.progress-bar-interrupted::-moz-progress-value { background: #F44336; }
    progress.progress-bar-interrupted::-webkit-progress-bar { background: #F44336; }
    progress.progress-bar-interrupted::-moz-progress-bar { background: #F44336; }
    progress.progress-bar-interrupted { background: #F44336; }    
&#10;    table.fastprogress { border-collapse: collapse; margin: 1em 0; font-size: 0.9em; }
    table.fastprogress th, table.fastprogress td { padding: 8px 12px; border: 1px solid #ddd; text-align: left; }
    table.fastprogress thead tr { background: #f8f9fa; font-weight: bold; }
    table.fastprogress tbody tr:nth-of-type(even) { background: #f8f9fa; }
</style>

<style>
    progress { appearance: none; border: none; border-radius: 4px; width: 300px;
        height: 20px; vertical-align: middle; background: #e0e0e0; }
&#10;    progress::-webkit-progress-bar { background: #e0e0e0; border-radius: 4px; }
    progress::-webkit-progress-value { background: #2196F3; border-radius: 4px; }
    progress::-moz-progress-bar { background: #2196F3; border-radius: 4px; }
&#10;    progress:not([value]) {
        background: repeating-linear-gradient(45deg, #7e7e7e, #7e7e7e 10px, #5c5c5c 10px, #5c5c5c 20px); }
&#10;    progress.progress-bar-interrupted::-webkit-progress-value { background: #F44336; }
    progress.progress-bar-interrupted::-moz-progress-value { background: #F44336; }
    progress.progress-bar-interrupted::-webkit-progress-bar { background: #F44336; }
    progress.progress-bar-interrupted::-moz-progress-bar { background: #F44336; }
    progress.progress-bar-interrupted { background: #F44336; }    
&#10;    table.fastprogress { border-collapse: collapse; margin: 1em 0; font-size: 0.9em; }
    table.fastprogress th, table.fastprogress td { padding: 8px 12px; border: 1px solid #ddd; text-align: left; }
    table.fastprogress thead tr { background: #f8f9fa; font-weight: bold; }
    table.fastprogress tbody tr:nth-of-type(even) { background: #f8f9fa; }
</style>

``` python
c = v.context('where does the entity graph get rebuilt?', sections=3, related=0, code=3, dir='..')
c.code, [r.breadcrumb for r in c.results if r.node_id is None]
```

    (3,
     ['shelf:papers › Attention Is All You Need',
      'shelf:sanskrit_fast › Vālmīki Rāmāyaṇa, Bālakāṇḍa',
      'repo › ../vishalakshi/core.py:397'])

Those sections are numbered alongside the prose ones and cite like them, so `ask` needed no changes
at all. A code citation just has no `node_id`, because its handle is a `path:line` on disk rather
than something `read()` can open.

`federate` goes wider: the vault, kosha, and ripgrep on the working tree. Three kinds of evidence
and three kinds of blindness. The vault embeds prose; kosha embeds identifiers with a code-trained
model, deliberately, because code embeds badly under a prose encoder; ripgrep embeds nothing and
sees the file as it is on disk right now, including the file nothing has indexed and the edit made a
minute ago. The legs share no vector space, so `federate` fuses their rankings with RRF and never
their distances. `symbol`, `where_to_add` and `grep` are on the [code page](03_code.ipynb).

``` python
L(v.grep('rrf_all', '..', limit=4)).attrgot('where')   # ripgrep, gitignore-aware
```

    ['README.md:501', 'README.md:809', 'README.md:822', 'README.md:832']

## Watches: keeping it current

An action is the name of an acquisition method, so anything you can file once you can file on a
schedule. `harvest` is one of them, which is what makes a page that renders from an internal JSON
API worth watching: fossick captures the calls the page makes, the vault picks the one carrying
records, follows its pagination, and files each record as its own retrievable section. See the
[acquire page](01_acquire.ipynb) for `apis`, `harvest` and `add_records`.

``` python
v.watch('https://example.com/changelog', action='url',     every='6h')
v.watch('late chunking retrieval',       action='web',     every='1d', n=5)
v.watch('Re-read the eval numbers',      action='remind',  every='1w')

L(v.watches()).map(lambda w: (w['action'], w['target'][:34], w['every'], w['params']))
```

    [('url', 'https://example.com/changelog', 21600.0, {}), ('web', 'late chunking retrieval', 86400.0, {'n': 5}), ('remind', 'Re-read the eval numbers', 604800.0, {})]

``` python
v.poll()      # run everything due; failures are recorded on the row, never raised
```

    [2026-08-11 07:27:54] INFO: Fetched (404) <GET https://example.com/changelog> (referer: https://www.google.com/)
    [2026-08-11 07:27:57] INFO: Fetched (200) <GET https://weaviate.io/blog/late-chunking> (referer: https://www.google.com/)
    [2026-08-11 07:27:57] INFO: Fetched (200) <GET https://arxiv.org/abs/2409.04701> (referer: https://www.google.com/)
    [2026-08-11 07:27:57] INFO: Fetched (200) <GET https://jina.ai/news/late-chunking-in-long-context-embedding-models/> (referer: https://www.google.com/)
    [2026-08-11 07:27:57] INFO: Fetched (200) <GET https://github.com/jina-ai/late-chunking> (referer: https://www.google.com/)
    [2026-08-11 07:27:57] INFO: Fetched (200) <GET https://medium.com/@visrow/chunking-strategies-for-rag-early-late-and-contextual-chunking-explained-with-code-71b88e4709f9> (referer: https://www.google.com/)

    {'checked': 3,
     'ran': 3,
     'results': [{'watch_id': 'd8d164b41ec4', 'action': 'url', 'target': 'https://example.com/changelog', 'status': 'skipped', 'took': 0.09, 'result': {'url': 'https://example.com/changelog', 'skipped': 'could not read the page (status 404)', 'status': 404}}, {'watch_id': 'e701ece6eb0d', 'action': 'web', 'target': 'late chunking retrieval', 'status': 'ok', 'took': 3.92, 'result': {'query': 'late chunking retrieval', 'n_found': 5, 'added': [{'doc_id': '5ff9f204a961b1d1', 'title': 'arxiv.org › abs › 2409[2409.04701] Late Chunking: Contextual Chunk Embeddings Using...arxiv.org › html › 2409Late Chunki', 'kind': 'web', 'nodes': 3, 'chunks': 6, 'url': 'https://arxiv.org/abs/2409.04701'}, {'doc_id': '504a7c1f1326cc02', 'title': 'weaviate.io › blog › late-chunkingLate Chunking: Balancing Precision and Cost in Long Context...medium.com › @visrow › c', 'kind': 'web', 'nodes': 13, 'chunks': 39, 'url': 'https://weaviate.io/blog/late-chunking'}, {'doc_id': 'd33cd761db418314', 'title': 'Chunking Strategies for RAG: Early, Late, and Contextual Chunking Explained (With Code) | by Vishal Mysore | Medium', 'kind': 'web', 'nodes': 27, 'chunks': 38, 'url': 'https://medium.com/@visrow/chunking-strategies-for-rag-early-late-and-contextual-chunking-explained-with-code-71b88e4709f9'}, {'doc_id': 'b1a6b84b6491cce9', 'title': 'GitHub - jina-ai/late-chunking: Code for explaining and evaluating late ...', 'kind': 'web', 'nodes': 6, 'chunks': 19, 'url': 'https://github.com/jina-ai/late-chunking'}, {'doc_id': '184c1b13d8f41da3', 'title': 'Late Chunking in Long-Context Embedding Models', 'kind': 'web', 'nodes': 2, 'chunks': 29, 'url': 'https://jina.ai/news/late-chunking-in-long-context-embedding-models/'}], 'dropped': []}}, {'watch_id': '51c6ced91d1d', 'action': 'remind', 'target': 'Re-read the eval numbers', 'status': 'ok', 'took': 0.0, 'result': {'doc_id': '18f38fd176761b4e', 'title': 'Re-read the eval numbers', 'kind': 'note', 'nodes': 2, 'chunks': 1}}],
     'next_due': 1786418874.2080588}

`poll()` is the tick. Call it from cron, a scheduler, or a frontend button. `remind` writes a note
with no network involved, and one dead URL never stops the loop, because a failure is recorded on the
row rather than raised.

## The rest

Each of these has its own page, and none of them is a decision you have to make to get started.

| page | what is on it |
|----|----|
| [core](00_core.ipynb) | [`Vault`](https://vedicreader.github.io/vishalakshi/core.html#vault) itself: shelves, `drop_shelf`, `context`, the entity graph, `document` |
| [acquire](01_acquire.ipynb) | `grab`, `url`, `web`, `crawl`, `arxiv`, `pdf`, `youtube`, `github`, `apis`, `harvest`, watches |
| [ask](02_ask.ipynb) | `ask`, `ask_doc`, citations, and the model plumbing |
| [code](03_code.ipynb) | kosha, `symbol`, `where_to_add`, `grep`, `federate` |
| [cli](04_cli.ipynb) | every [`Vault`](https://vedicreader.github.io/vishalakshi/core.html#vault) method as a command; `--help` generated from the signature |
| [mcp](05_mcp.ipynb) | `vishalakshi-mcp`, and the same methods as tools |
| [extract](06_extract.ipynb) | `categorize`, `extract`, `extract_all`, the schemas |
| [concepts](07_concepts.ipynb) | doctypes, `reshelf`, and what the vault decides about a document |

``` sh
vishalakshi grab https://example.com/post     # or a file, a directory, an arXiv id, a YouTube URL
vishalakshi ask "why does late chunking help"
```

`$VISHALAKSHI_VAULT` picks the vault file, `$VISHALAKSHI_MODEL` the model `ask` uses, and
`$VISHALAKSHI_OFFLINE` forces the hashing encoder. `vishalakshi-mcp` exposes the same methods to any
MCP client:

``` json
{"mcpServers": {"vishalakshi": {"command": "vishalakshi-mcp",
                                "env": {"VISHALAKSHI_VAULT": "~/.vishalakshi/vault.db"}}}}
```

## Development

The notebooks in `nbs/` are the source; the modules are generated.

``` sh
pip install -e '.[all]'
nbdev-prepare
```
